/** Per-user accent colors, cycled by roster index. */
export const USER_COLORS: string[] = [
  "#39d353",
  "#f0a500",
  "#58a6ff",
  "#ff7b72",
  "#bc8cff",
  "#3fb950",
  "#ffa657",
];

/** Auto-sync cadence. */
export const AUTO_SYNC_MS = 10 * 60 * 1000; // 10 minutes

/** localStorage key for the visitor's recently-opened boards. */
export const LS_RECENT_BOARDS = "lb-recent-boards";
